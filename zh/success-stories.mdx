---
title: 成功案例
mode: wide
---

import { SuccessStoryCard } from '/snippets/zh/success-story-card.mdx';
import data from '/snippets/zh/success-story-card-data.mdx'

全球各地的公司和个人借助 X 开发者平台进行创意创新、获取宝贵洞察，并塑造未来。


<div id="browse-success-stories">
  ## 浏览成功案例
</div>

<CardGroup cols={3}>
{data.map((item, index) => (
    <SuccessStoryCard key={index} title={item.title} img={item.image} href={item.path}/>
))}
</CardGroup>