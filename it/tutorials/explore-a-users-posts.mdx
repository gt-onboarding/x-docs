---
title: Esplora i Post e le menzioni di un utente con l’API X v2
description: "Gli endpoint della timeline dei Post di un utente e della timeline delle menzioni consentono agli sviluppatori di ottenere i Post pubblici pubblicati da un utente o che lo menzionano."
sidebarTitle: Esplora i Post di un utente
mode: wide
---

<div id="introduction">
  ## Introduzione
</div>

Sebbene l’[endpoint di ricerca recente](/it/x-api/posts/search/introduction) consenta di ottenere solo i Post pubblicati negli ultimi 7 giorni, gli endpoint della timeline dei Post dell’utente e della timeline delle menzioni dell’utente permettono di recuperare Post e menzioni anteriori agli ultimi 7 giorni, per un utente autorizzato (utilizzando l’ID utente). Gli sviluppatori possono usare questi endpoint per analizzare argomenti, entità e sentiment dei Post dalla timeline o dalle menzioni di un utente. In questo tutorial mostreremo come esplorare i Post e le menzioni di un utente utilizzando gli endpoint della timeline dei Tweet dell’utente e della timeline delle menzioni dell’utente.
 

<div id="prerequisites">
  ## Prerequisiti
</div>

- Per utilizzare gli endpoint della timeline dei post dell’utente e della timeline delle menzioni, è necessario disporre di un account sviluppatore valido.
- È inoltre necessario avere un [Progetto](/it/resources/fundamentals/projects) già creato.
- Devi esserti [registrato](https://developer.x.com/en/portal/petition/essential/basic-info) per un account sviluppatore e aver attivato la [nuova esperienza del portale sviluppatori](https://developer.x.com/en/portal/opt-in.html).
- L’accesso è disponibile con chiavi e token attivi per un’App sviluppatore collegata a un [Progetto](/it/resources/fundamentals/projects) creato nel [portale sviluppatori](/it/resources/fundamentals/developer-portal).
- Un Bearer Token dalla tua App nel [portale sviluppatori di X](/it/resources/fundamentals/developer-portal).
- Se non disponi di un account sviluppatore approvato, puoi [presentare domanda](https://developer.x.com/en/apply-for-access).

<div id="approved-developer-account">
  ## Account sviluppatore approvato
</div>

Se non ne disponi ancora, puoi [fare domanda](https://developer.x.com/en/apply-for-access).

<div id="create-a-project-and-connect-an-app">
  ## Crea un Progetto e collega un'App
</div>

Nel [portale sviluppatori](https://developer.x.com/en/portal/dashboard), fai clic per creare un nuovo Progetto.

<Frame>
  <img src="/images/projects.png.twimg.1920.png" />
</Frame>

Assegna un nome, seleziona il caso d’uso appropriato e fornisci una descrizione del Progetto. Quindi puoi creare una nuova App oppure collegare un’[App](/it/resources/fundamentals/developer-apps) esistente (un’App è un contenitore per le chiavi API necessarie per effettuare richieste HTTP all’API X).

<Frame>
  <img src="/images/add-your-app.png.twimg.1920.png" />
</Frame>

Fai clic su “create a new App instead” e assegna un nome alla tua App per crearne una nuova.

<Frame>
  <img src="/images/last-step-name-your-app.png.twimg.1920.png" />
</Frame>

Dopo aver fatto clic su Completa, riceverai le chiavi API e il bearer token che potrai utilizzare per connetterti ai nuovi endpoint dell’API X v2.

<Frame>
  <img src="/images/here-are-your-keys.png.twimg.1920.png" />
</Frame>

Fai clic sul (+) accanto a API key, API secret key e Bearer token e copia questi valori in un luogo sicuro sulla tua macchina locale. Ti serviranno per effettuare le chiamate API nel passaggio successivo.

Nota: le chiavi nello screenshot sopra sono nascoste, ma nel tuo portale sviluppatori potrai vedere i valori effettivi di API key, API secret key e Bearer token.
 

<div id="how-to-get-the-user-id-for-a-user-to-use-in-the-user-tweet-timeline-and-user-mention-timeline-endpoints">
  ## Come ottenere l’ID di un utente da usare negli endpoint della timeline dei Post dell’utente e delle menzioni dell’utente
</div>

Gli endpoint della timeline dei Post dell’utente e delle menzioni dell’utente consentono di ottenere Post utilizzando l’ID utente. Per ricavare l’ID utente a partire da un username, puoi usare il nuovo [endpoint di ricerca utente v2](/it/x-api/users/lookup/quickstart/user-lookup). Sostituisci USER\_NAME con lo username desiderato e XXXXXX con il tuo bearer token ottenuto in precedenza

```bash
  curl --request GET 'https://api.x.com/2/users/by/username/USER_NAME --header 'Authorization: Bearer XXXXXX'
```

Vedrai l’ID utente nella risposta, come mostrato di seguito:

```json
{
   "data": {
       "id": "2244994945",
       "name": "Developers",
       "username": "XDevelopers"
   }
}
```

<div id="connecting-to-the-user-tweet-timeline-and-user-mention-timeline-endpoints">
  ## Connessione agli endpoint della timeline dei post dell’utente e della timeline delle menzioni
</div>

Per ottenere la timeline dei post di un utente, esegui il seguente comando curl nel tuo terminale (assicurati di sostituire USER\_ID con l’ID dell’utente desiderato e XXXXXX con il tuo bearer token ottenuto in precedenza)

```bash
curl --request GET 'https://api.x.com/2/users/USER_ID/tweets' --header 'Authorization: Bearer XXXXXX'
```

Vedrai che la risposta JSON a queste richieste contiene per impostazione predefinita l’ID e il testo dei post (vedi esempio sotto).

```yaml
{
   "id": "1334200897081987072",
   "text": "👀 If you are new to the X API v2, check out this step-by-step guide to making your first request https://t.co/4rZqThpSbp"
}
```

Se vuoi che vengano restituiti campi aggiuntivi nella risposta (ad esempio informazioni sull’utente, campi aggiuntivi dei post come le annotazioni di contesto, ecc.), devi specificarli esplicitamente nella richiesta. Scopri come farlo nella [guida all’uso di campi ed espansioni](/it/x-api/fundamentals/data-dictionary#how-to-use-fields-and-expansions).

Puoi anche ottenere questi post utilizzando i linguaggi di programmazione che preferisci. Consulta il nostro codice di esempio in Python, Node (JavaScript), Java e Ruby per gli endpoint della timeline dei post dell’utente e della timeline delle menzioni nel nostro [repository GitHub](https://github.com/xdevplatform/Twitter-API-v2-sample-code).
 

<div id="exploring-the-users-posts">
  ## Esplorare i Post dell’utente
</div>

Una volta che sai come ottenere i Post utilizzando gli endpoint user Tweet timeline e user mention timeline, puoi iniziare a esplorare i loro Post. Ad esempio, se volessi identificare le entità nominate comuni presenti nelle menzioni di un utente, puoi procedere così:

Nella richiesta API, specifica che desideri l’oggetto context\_annotations restituito nelle risposte dei Tweet:

```bash
curl --request GET 'https://api.x.com/2/users/USER_ID/mentions?tweet.fields=context_annotations' --header 'Authorization: Bearer XXXXXX'
```

Nella risposta, vedrai se nelle menzioni sono presenti entità nominate. Ecco un esempio:

```json
{
   "domain": {
       "id": "47",
       "name": "Brand",
       "description": "Brands and Companies"
   },
   "entity": {
       "id": "783214",
       "name": "X"
   }
}
```

Se volessi vedere quali entità popolari compaiono nelle tue menzioni, potresti mantenerne un conteggio analizzando ciascun Tweet nelle menzioni.

Se volessi esplorare l’URL dell’immagine di anteprima per tutti i Post nella tua timeline che contengono contenuti multimediali, puoi procedere così:

Nella richiesta API, specifica che desideri preview\_image\_url nei campi tweet.media e le espansioni attachments.media\_keys:

```bash
curl --request GET 'https://api.x.com/2/users/2244994945/mentions?max_results=100&media.fields=preview_image_url&expansions=attachments.media_keys' --header 'Authorization: Bearer XXXXXX'
```

Nella risposta, vedrai preview\_image\_url nell’oggetto includes come mostrato di seguito:

```json
{
   "includes": {
       "media": [
           {
               "media_key": "16_1334657439640121344",
               "preview_image_url": "https://pbs.twimg.com/tweet_video_thumb/EoWn3rqU8AAtFWL.jpg",
               "type": "animated_gif"
           }
       ]
   }
}
```

Una volta compreso come navigare i Post di un utente, puoi anche utilizzare altre API e servizi per fare di più con i Post. Di seguito trovi alcune risorse utili quando utilizzi gli endpoint user Tweet timeline e user mention timeline.

<div id="resources">
  ## Risorse
</div>

- Scopri di più sugli endpoint della [timeline dei post dell’utente e della timeline delle menzioni dell’utente](/it/x-api/posts/timelines/introduction).
- Consulta la [documentazione di riferimento dell’API per l’endpoint della timeline dei post dell’utente](/it/x-api/posts/user-posts-timeline-by-user-id) per approfondire le funzionalità disponibili.
- Consulta la [documentazione di riferimento dell’API per l’endpoint della timeline delle menzioni dell’utente](/it/x-api/posts/user-mention-timeline-by-user-id) per approfondire le funzionalità disponibili.
- Trova ispirazione leggendo gli altri nostri [tutorial](/it/tutorials).