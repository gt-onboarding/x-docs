---
title: Casi di successo
mode: wide
---

import { SuccessStoryCard } from '/snippets/it/success-story-card.mdx';
import data from '/snippets/it/success-story-card-data.mdx'

Aziende e persone in tutto il mondo hanno utilizzato la X Developer Platform per innovare in modo creativo, ottenere preziose informazioni e plasmare il futuro.


<div id="browse-success-stories">
  ## Scopri le storie di successo
</div>

<CardGroup cols={3}>
{data.map((item, index) => (
    <SuccessStoryCard key={index} title={item.title} img={item.image} href={item.path}/>
))}
</CardGroup>