---
title: Gestione dei segnalibri
sidebarTitle: Gestione dei segnalibri
---

import { Button } from '/snippets/it/button.mdx';

<div id="getting-started-with-the-manage-bookmarks-endpoints">
  ## Introduzione agli endpoint per la gestione dei segnalibri
</div>

Questa guida rapida ti aiuterà a inviare la tua prima richiesta agli endpoint per la gestione dei segnalibri utilizzando [Postman](/it/tutorials/postman-getting-started).

Se desideri vedere esempi di codice in diverse lingue, visita il nostro repository GitHub [X API v2 sample code](https://github.com/xdevplatform/Twitter-API-v2-sample-code).

<Note>
  **Prerequisiti**

  Per completare questa guida, avrai bisogno di un set di [keys and tokens](/it/resources/fundamentals/authentication) per autenticare la tua richiesta. Puoi generare queste chiavi e token seguendo questi passaggi:

  - [Registrati per un account sviluppatore](https://developer.x.com/en/apply-for-access) e attendi l’approvazione.
  - Crea un [Project](/it/resources/fundamentals/projects) e una [developer App](/it/resources/fundamentals/developer-apps) associata nel portale per sviluppatori.
  - Vai alla pagina “Keys and tokens” della tua App per generare le credenziali necessarie. Assicurati di salvare tutte le credenziali in un luogo sicuro.
</Note>

<div id="steps-to-build-a-manage-bookmarks-request">
  ### Passaggi per creare una richiesta per gestire i Segnalibri
</div>

**Primo passaggio: inizia con uno strumento o una libreria**

Esistono diversi strumenti, esempi di codice e librerie che puoi utilizzare per effettuare una richiesta a questo endpoint, ma qui useremo Postman per semplificare il processo.

Per caricare la raccolta API X v2 nel tuo ambiente Postman, fai clic sul seguente pulsante:

<Button href="https://app.getpostman.com/run-collection/9956214-784efcda-ed4c-4491-a4c0-a26470a67400">
  Aggiungi API X v2 a Postman
</Button>

Una volta caricata la raccolta API X v2 in Postman, vai alla cartella “Bookmarks” e seleziona “Create a Bookmark”.

**Secondo passaggio: autentica la tua richiesta**

Per effettuare una richiesta corretta a questo endpoint, dovrai utilizzare [OAuth 2.0 Authorization Code Flow with PKCE](/it/resources/fundamentals/authentication#oauth-2-0-authorization-code-flow-with-pkce-3). Puoi generare un token di accesso direttamente in Postman.

Vai alla scheda “Authorization” e seleziona “OAuth 2.0”.

In questa scheda, assicurati di seguire questi passaggi:

1. Assegna un nome al token

2. Seleziona come Grant Type Authorization Code (with PKCE)

3. Aggiorna i parametri:

   **Callback URL** - [https://www.example.com](https://www.example.com/)

   Deve corrispondere alla callback URL impostata nella pagina delle impostazioni di autenticazione nel Developer Portal.

   **Auth URL** - [https://x.com/i/oauth2/authorize](https://x.com/i/oauth2/authorize)

   **Access Token URL** - [https://api.x.com/2/oauth2/token](https://api.x.com/2/oauth2/token)

   **Client ID** - Copia e incolla l’ID client OAuth 2.0 dal Developer Portal

   **Client Secret** - Copia e incolla il Client Secret OAuth 2.0 dal Developer Portal. Ti servirà solo se stai utilizzando un’App di tipo confidential client.

4. Aggiorna gli scope con i seguenti valori: post.read users.read bookmark.write

5. Compila il campo state con “State”

6. Clicca su “Generate Token”

7. Premi l’icona di salvataggio per salvare le modifiche alla cartella.

Potresti ricevere un messaggio che indica che non hai effettuato l’accesso a X. In tal caso, devi accedere all’account X, all’interno di Postman, per conto del quale stai tentando di pubblicare.

**Terzo passaggio: specifica un utente**

Con questo endpoint, devi specificare l’ID dell’utente per il quale desideri ricevere i follower nella risposta. Ad esempio, l’ID utente di @XDevelopers è `2244994945`.

In Postman, vai alla scheda “Params” e inserisci il tuo ID o quello di un utente autenticato come valore per il parametro id.

|     |     |     |
| :--- | :--- | :--- |
|     | Key | Value |
|     | id  | `2244994945` |

**Quarto passaggio: specifica un Post che vuoi aggiungere ai Segnalibri**

Vai alla scheda “Body” e assicurati che l’ID del Post sia impostato su quello che desideri salvare nei Segnalibri. Il payload JSON dovrebbe essere simile al seguente.

```
      {"tweet_id": "1460323737035677698"}
```

**Quinto passaggio: invia la richiesta e verifica la risposta**

Una volta configurato tutto, premi il pulsante "Send" e riceverai una risposta simile al seguente esempio:

```
{
   "data": {
       "bookmarked": true
   }
}
```

Per eliminare un Post, seleziona la richiesta “Remove a Bookmark”, presente nella cartella “Bookmarks” della raccolta API X v2 caricata in Postman. Per prima cosa, specifica l’ID dell’utente per conto del quale stai effettuando la richiesta come valore per la colonna “`id`”. Questo endpoint richiede anche l’ID del Post che desideri eliminare. Quindi, nella scheda “Params”, inserisci l’ID del Post da eliminare come valore per la colonna “`tweet_id`”.

Quando la richiesta di eliminazione va a buon fine, riceverai una risposta simile al seguente esempio:

```
{
   "data": {
       "bookmarked": false
   }
}
```